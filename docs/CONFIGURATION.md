# Configuration — FIB Scalper

Primary strategy settings are in `config/settings.yaml` under:

```yaml
strategy:
  name: "FIB"
  fibonacci:
    mode: "reversal"
```

Modes: `reversal`, `trend_pullback`, `confluence`.
